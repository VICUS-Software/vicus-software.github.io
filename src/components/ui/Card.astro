---
interface Props {
  class?: string;
  hover?: boolean;
}

const { class: className = '', hover = true } = Astro.props;
const classes = ['card', hover && 'hover-lift', className].filter(Boolean).join(' ');
---

<div class={classes}>
  <slot />
</div>

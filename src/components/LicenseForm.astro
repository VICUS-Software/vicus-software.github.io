---
import { getLangFromUrl, useTranslations } from '../i18n/utils';
import type { Language } from '../i18n/languages';
import { HUBSPOT_CONFIG } from '../config/hubspot';

const lang = getLangFromUrl(Astro.url) as Language;
const t = useTranslations(lang);
---

<form
  id="license-form"
  class="space-y-6"
  data-hubspot-portal={HUBSPOT_CONFIG.portalId}
  data-hubspot-form={HUBSPOT_CONFIG.formIds.license}
>
  <div class="grid md:grid-cols-2 gap-6">
    <div class="form-group">
      <label for="lic-firstname" class="form-label">
        {t('form.firstname')} <span class="text-primary">*</span>
      </label>
      <input
        type="text"
        id="lic-firstname"
        name="firstname"
        required
        class="form-input"
      />
    </div>
    <div class="form-group">
      <label for="lic-lastname" class="form-label">
        {t('form.lastname')} <span class="text-primary">*</span>
      </label>
      <input
        type="text"
        id="lic-lastname"
        name="lastname"
        required
        class="form-input"
      />
    </div>
    <div class="form-group">
      <label for="lic-email" class="form-label">
        {t('form.email')} <span class="text-primary">*</span>
      </label>
      <input
        type="email"
        id="lic-email"
        name="email"
        required
        class="form-input"
      />
    </div>
  </div>

  <div class="grid md:grid-cols-2 gap-6">
    <div class="form-group">
      <label for="lic-company" class="form-label">
        {t('form.company')} <span class="text-primary">*</span>
      </label>
      <input
        type="text"
        id="lic-company"
        name="company"
        required
        class="form-input"
      />
    </div>
    <div class="form-group">
      <label for="lic-phone" class="form-label">{t('form.phone')}</label>
      <input
        type="tel"
        id="lic-phone"
        name="phone"
        class="form-input"
      />
    </div>
  </div>

  <div class="form-group">
    <label for="lic-product" class="form-label">
      {lang === 'de' ? 'Produkt' : 'Product'} <span class="text-primary">*</span>
    </label>
    <select id="lic-product" name="product" required class="form-input">
      <option value="">{lang === 'de' ? 'Bitte wählen...' : 'Please select...'}</option>
      <option value="buildings">VICUS BUILDINGS</option>
      <option value="districts">VICUS DISTRICTS</option>
      <option value="both">{lang === 'de' ? 'Beide Produkte' : 'Both products'}</option>
    </select>
  </div>

  <div class="form-group">
    <label for="lic-type" class="form-label">
      {lang === 'de' ? 'Lizenztyp' : 'License Type'} <span class="text-primary">*</span>
    </label>
    <select id="lic-type" name="licenseType" required class="form-input">
      <option value="">{lang === 'de' ? 'Bitte wählen...' : 'Please select...'}</option>
      <option value="single">{lang === 'de' ? 'Einzellizenz' : 'Single license'}</option>
      <option value="team">{lang === 'de' ? 'Teamlizenz' : 'Team license'}</option>
      <option value="enterprise">{lang === 'de' ? 'Unternehmenslizenz' : 'Enterprise license'}</option>
      <option value="academic">{lang === 'de' ? 'Akademische Lizenz' : 'Academic license'}</option>
    </select>
  </div>

  <div class="form-group">
    <label for="lic-message" class="form-label">{t('form.message')}</label>
    <textarea
      id="lic-message"
      name="message"
      rows="4"
      class="form-input resize-none"
      placeholder={lang === 'de' ? 'Ihre Anforderungen...' : 'Your requirements...'}
    ></textarea>
  </div>

  <div id="license-form-status" class="hidden p-4 rounded-lg"></div>

  <button type="submit" class="btn btn-primary w-full md:w-auto">
    {lang === 'de' ? 'Lizenz anfragen' : 'Request License'}
  </button>
</form>


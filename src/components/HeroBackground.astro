<!-- Subtle gradient mesh background -->
<div class="hero-mesh absolute inset-0">
  <div class="mesh-blob mesh-blob-1"></div>
  <div class="mesh-blob mesh-blob-2"></div>
  <div class="mesh-blob mesh-blob-3"></div>
</div>

<style>
  .hero-mesh {
    overflow: hidden;
    filter: blur(80px);
  }

  .mesh-blob {
    position: absolute;
    border-radius: 50%;
    will-change: transform;
  }

  .mesh-blob-1 {
    width: 50vw;
    height: 50vw;
    max-width: 700px;
    max-height: 700px;
    top: -10%;
    left: -10%;
    background: radial-gradient(circle, rgba(106, 220, 173, 0.25) 0%, transparent 70%);
    animation: drift1 14s ease-in-out infinite;
  }

  .mesh-blob-2 {
    width: 45vw;
    height: 45vw;
    max-width: 600px;
    max-height: 600px;
    bottom: -15%;
    right: -5%;
    background: radial-gradient(circle, rgba(20, 70, 160, 0.2) 0%, transparent 70%);
    animation: drift2 18s ease-in-out infinite;
  }

  .mesh-blob-3 {
    width: 35vw;
    height: 35vw;
    max-width: 500px;
    max-height: 500px;
    top: 40%;
    left: 35%;
    background: radial-gradient(circle, rgba(106, 220, 173, 0.12) 0%, transparent 70%);
    animation: drift3 20s ease-in-out infinite;
  }

  @keyframes drift1 {
    0% { transform: translate(0, 0) scale(1); }
    25% { transform: translate(30%, 20%) scale(1.1); }
    50% { transform: translate(50%, -10%) scale(0.95); }
    75% { transform: translate(10%, 30%) scale(1.05); }
    100% { transform: translate(0, 0) scale(1); }
  }

  @keyframes drift2 {
    0% { transform: translate(0, 0) scale(1); }
    25% { transform: translate(-35%, -20%) scale(1.08); }
    50% { transform: translate(-15%, 25%) scale(0.92); }
    75% { transform: translate(20%, -30%) scale(1.05); }
    100% { transform: translate(0, 0) scale(1); }
  }

  @keyframes drift3 {
    0% { transform: translate(0, 0) scale(1); }
    25% { transform: translate(25%, -25%) scale(1.12); }
    50% { transform: translate(-30%, 15%) scale(0.9); }
    75% { transform: translate(-10%, -20%) scale(1.08); }
    100% { transform: translate(0, 0) scale(1); }
  }
</style>
